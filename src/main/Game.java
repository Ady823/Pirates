public class Game{
    int numPlayers = 2; //player1.playerCount();
    int losingScore = 60 / numPlayers + 1;

    public Game(){
    round1();
    }

    }
    private String round1(){ 
        for (int i = 0; i <= numPlayers; i++) {
            player(i) cards[0] = deck[0]

            for (int j = 0; j < getArray().length; j--){
                if (deck[j] != 0){

                }
            }
        }
    }

}